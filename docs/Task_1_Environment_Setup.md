# LegalEase – Development Environment Setup

## Activity 1.3: Set Up the Development Environment

### 1. Python Environment

Python 3.14.5 is installed and available for the project.

A virtual environment named `venv` was created and activated.

### 2. Required Dependencies

The following dependencies were installed:

- FastAPI
- Uvicorn
- Streamlit
- python-docx
- fpdf
- Pillow
- requests
- google-generativeai
- python-dotenv

The installed packages were verified successfully.

### 3. Environment Configuration

A `.env` file was created for storing the Gemini API key securely.

### 4. Project Structure

The LegalEase project contains:

- `backend/`
- `frontend/`
- `docs/`
- `.env`
- `requirements.txt`

### 5. Verification

All required Python packages were successfully imported and the message
`Environment setup complete` was displayed.

The development environment is ready for LegalEase application development.